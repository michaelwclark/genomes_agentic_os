# Specs

Use this folder for new source-package future-work specs and planning backlog
items.

Existing numbered files under `PLANS/` remain legacy source material during the
migration window. Do not bulk-move them without a separate migration pass that
updates references and installed-runtime copies.
