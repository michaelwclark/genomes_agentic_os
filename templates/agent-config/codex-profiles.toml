# Codex profile template for Genome's Agentic OS.
# Keep OS metadata in codex-profile-manifest.yml. Keep durable prompt behavior
# in AGENTS.md, ROUTER.md, CONTEXT.md, RULES.md, TOOLS.md, and MEMORY.md.

[profiles.global_user_harness]
model = "gpt-5.2"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.agentic_os_root]
model = "gpt-5.2"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.customer_os_root]
model = "gpt-5.2"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.domain_or_lane]
model = "gpt-5.2"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.workflow_or_task]
model = "gpt-5.2"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[profiles.automation]
model = "gpt-5.2"
model_reasoning_effort = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
