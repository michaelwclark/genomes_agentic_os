interface:
  display_name: PR Review
  short_description: Canonical others'-PR review, batch reporting, engineering-health,
    and authority-aware standard squash-merge wo
  default_prompt: Use the Agentic OS PR Review skill.
policy:
  allow_implicit_invocation: true
