interface:
  display_name: Domain Setup
  short_description: Create or improve a domain room inside the OS.
  default_prompt: Use the Agentic OS Domain Setup skill.
policy:
  allow_implicit_invocation: true
