interface:
  display_name: Workflow Builder
  short_description: Create or refine reusable workflow contracts.
  default_prompt: Use the Agentic OS Workflow Builder skill.
policy:
  allow_implicit_invocation: true
