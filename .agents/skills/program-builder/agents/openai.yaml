interface:
  display_name: Program Builder
  short_description: Create or refine OSProgram and InstanceOSProgram context bundles.
  default_prompt: Use the Agentic OS Program Builder skill.
policy:
  allow_implicit_invocation: true
