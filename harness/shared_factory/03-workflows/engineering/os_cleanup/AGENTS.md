# OS Cleanup Agent Entry Point

Read `ROUTER.md`, `CONTEXT.md`, `RULES.md`, and `TOOLS.md`, then operate the
receipt-first workflow in `workflow.md`. Route to the owning project before
touching an item. Preserve the strictest parent/project safety rule.

