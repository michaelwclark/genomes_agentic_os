# Router

Route one delivered Auto-Dev item to `/auto-dev-health`. Route a bounded general
worktree/work-item reconciliation to `$os-cleaner`. Route host-wide observation
to OS Health. This workflow owns cleanup policy, not merge or deployment truth.
