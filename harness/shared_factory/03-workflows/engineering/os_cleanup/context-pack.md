# Context pack: OS cleanup

Load the owning root/domain/project contracts, project development and
environment-access configuration, `autodev.json` when present, canonical work
state, worktree active/closed registries, verified merged-pull-request readback
with exact merge revision, Closeout
receipts, current `git status --porcelain`, root `REOPEN.md`, target-local
runtime definition, packet artifacts, and active-index projections. Use the
strictest applicable safety rule.
