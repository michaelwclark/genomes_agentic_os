# Work Item Archive Tools

Use `agentic-os work-item-archive --root <root> --dry-run` to inspect eligibility.
Use the same command with `--apply` for the scheduled mutation. Inspect receipts
under `harness/shared_factory/06-runs-and-logs/work-item-archive/`.
