# Work Item Archive Rules

- Archive only direct children of a canonical `work-items/` root.
- Never archive `99-archived/`, legacy lane directories, or packets with `REOPEN.md`.
- Require a terminal state and retention age before moving a packet.
- Keep packet contents intact and migrate registered work-state paths.
- Emit a receipt for every dry run and apply run.
