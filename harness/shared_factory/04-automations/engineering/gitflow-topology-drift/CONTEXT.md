# Context

Load the routed `project.yml`, the F2 GitFlow topology contract, the current
branch registry when configured, exact remote branches, and open PR bases.
Keep raw remote evidence local and emit only scrubbed proposals.
