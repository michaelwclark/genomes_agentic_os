# Runbook

This automation is dark. A manual observation run may gather fresh read-only
inputs, run F2 for each family, deduplicate missing-target proposals, and write
one local receipt. Stop if any configured registry is stale or unavailable.
