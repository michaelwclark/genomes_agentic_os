# Rules

- Disabled and proposal-only until separately approved.
- F2 is the only topology interpreter.
- No external writes and no schedule creation.
- Every proposal names the tracker, repository, source SHA, missing target,
  reason, idempotency key, and the command that would plan it through M4.
