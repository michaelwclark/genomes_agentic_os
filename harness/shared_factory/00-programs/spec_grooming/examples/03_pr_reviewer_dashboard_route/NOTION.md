# Notion Projection

Update the existing PR health or dashboard documentation page after verifying
Genome's Notion. Do not create a parallel dashboard report unless the route
decision is explicitly `create_new`.

