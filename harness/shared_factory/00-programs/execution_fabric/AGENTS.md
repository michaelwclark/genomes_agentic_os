# Execution Fabric Agent Entry Point

Read `ROUTER.md`, `CONTEXT.md`, `RULES.md`, and `TOOLS.md` before changing this
program. The program is installed in every Agentic OS instance but is inactive
until an operator explicitly enables it.
