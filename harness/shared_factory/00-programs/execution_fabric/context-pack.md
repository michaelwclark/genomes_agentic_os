# Execution Fabric Context Pack

Load `program.md`, `components.yml`, `config.toml`, `RULES.md`, and the owning
installed runtime configuration before planning activation or backend work.
Include current queue depth, worker/provider capacity, stale claims, producers,
and rollback requirements in any implementation handoff.
