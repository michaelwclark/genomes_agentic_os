# Project Domain Intelligence

Read `ROUTER.md`, `CONTEXT.md`, `RULES.md`, and `TOOLS.md` before using this
program. The shared program owns contracts and retrieval policy; an instance
program owns a project's configured domain surface.
