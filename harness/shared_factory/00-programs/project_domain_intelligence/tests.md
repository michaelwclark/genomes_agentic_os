# Tests: Project Domain Intelligence

- Create: program and LOS Django instance manifests remain linked by definition ID.
- Refresh: an observe-only run writes a deterministic receipt and no article.
- Retrieve: entrypoints can name the context receipt consumed.
- Drift: changed-path input yields stable source and path evidence.
- Rollback: instance documentation names the previous approved article revision.
- Validate: `python -m pytest -q tests/test_project_domain_intelligence.py`.
