# LOS Django Domain Intelligence Context

Load the linked shared program first. Use the LOS Django domain registry and
focused articles only as evidence aids; current source, tests, configuration,
and runtime observations remain authoritative.
