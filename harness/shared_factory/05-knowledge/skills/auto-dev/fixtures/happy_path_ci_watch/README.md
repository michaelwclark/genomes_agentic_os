# Happy Path CI Watch Fixture

Synthetic FLYWL-0000 smoke run artifacts captured after reaching `ci_watch`. The original active work item was removed from `02-active`; this fixture intentionally omits the stale `lock.json`.

Canonical state file: `auto-dev/state.json`. Legacy prototype state is preserved as `auto-dev/state.legacy.yml` for migration coverage.
