# Environment access policy

This cross-cutting plane tells Auto-Dev how to reach hosts, VPN-gated systems,
cloud environments, and remote runtimes safely. It references routed host and
environment owners; it never stores credentials.
