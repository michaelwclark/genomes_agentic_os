# QA Gates Policy Plane

These 1-N Markdown files define evidence required after implementation. They
compose root → domain → project independently from code standards because QA
has different timing, consumers, and output artifacts.
