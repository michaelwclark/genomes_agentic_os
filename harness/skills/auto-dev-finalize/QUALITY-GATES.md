# Moved: Quality Gates

2026-07-19: the canonical gate list is the composable DEV_STANDARDS contract
at `harness/shared_factory/05-knowledge/dev_standards/` (README.md defines
root -> domain -> project loading; one markdown per focus; project addenda
under `<project>/config/dev_standards/*.md`). Every mission enumerates the folders
on each run, so dropping a new markdown there extends auto-dev,
auto-dev-finalize, and pr-review with no registration. Do not add content
back to this file.

The previous `quality-gates` path remains a compatibility alias only.
