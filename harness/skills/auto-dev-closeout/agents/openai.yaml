interface:
  display_name: "Auto-Dev Closeout"
  short_description: "Merge, deploy, validate, reconcile, and close"
  default_prompt: "Use $auto-dev-closeout to finish this ready change through verified delivery and cleanup."
policy:
  allow_implicit_invocation: true
