---
name: auto-dev-develop
description: Friendly Auto-Dev name for implementing and locally validating one planned work item in its isolated worktree through the canonical Auto-Dev Implementation owner.
---

# Auto-Dev Develop

Delegate unchanged to `$auto-dev-implementation`. Read the same task state,
worktree, policy receipt, frozen context selection, and plan; do not create an alias-owned state packet.
For an LOS Rules Engine context, inspect its frozen status first. Read bound
contract, dictionary, checks, coverage, redundancy, snapshot, and findings only
when the receipt says `loaded` and names concrete artifact hashes. Preserve
`kit-unavailable` or `insufficient-evidence` as limitations; neither this route
nor a candidate grants live rule/config mutation.
Record the workflow as `develop` in `autodev.json` after canonical
`local_validation` evidence exists.
