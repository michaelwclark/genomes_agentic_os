# Retired implementation

The duplicate Graybeard `pull-request` review policy was retired on
2026-07-19. The public skill name remains as a compatibility alias to canonical
`pr-review`; the retained `pull-request-*` specialist reviewers are still
available to that workflow.

The original directory is preserved in work item 063 under
`artifacts/consolidation/retired-legacy-skills/pull-request-original/` with its
aggregate hash recorded in the retirement receipt.
