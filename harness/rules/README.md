# Harness Rules

Reserved for reusable rule packs that should be mirrored into installed Agentic
OS roots.

Core operating rules are currently generated into `harness/RULES.md`, domain
`RULES.md` files, project `RULES.md` files, and `harness/registries/rules.yml`.
Add files here only for packaged rule sets that need their own versioned assets.
