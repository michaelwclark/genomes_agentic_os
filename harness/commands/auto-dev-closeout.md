# `/auto-dev-closeout`

Reconcile verified merge, deployment, tracker, and provider truth and prove
canonical delivery complete. This records the cleanup decision but leaves
resource removal and the finished-packet move to `/auto-dev-health`. Use
`$auto-dev-closeout` and `agentic-os develop stage --stage closeout`.
