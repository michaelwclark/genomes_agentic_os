# OS Auto Add Feature

Compatibility command: `/auto-add-feature`

This is `/auto-add-spec --type feature`. Follow
`harness/commands/os-auto-add-spec.md` and the canonical `spec-engine` skill.
Do not create a separate feature-intake row, packet, lifecycle, or tracker sync.
