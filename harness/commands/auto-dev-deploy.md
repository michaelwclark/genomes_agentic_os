# `/auto-dev-deploy`

Deploy or monitor the exact merged artifact through the project owner and
verify deployed behavior. Use `$auto-dev-deploy`, then record the exact
revision/artifact readback with `agentic-os develop stage --stage deploy`.
