# Harness Plugins

Reserved for packaged harness plugins that should be mirrored into installed
Agentic OS roots.

Plugin availability is tracked in `harness/registries/plugins.yml`. This folder
can remain otherwise empty until a plugin is owned and shipped by this package.
