# Harness Libraries

Reserved for reusable harness libraries and helper assets owned by this package.

Library capabilities such as `context_mode` and `unified_memory` are listed in
`harness/registries/libraries.yml`; they may point to external services or tools
instead of files in this folder.
