{
  "specMarkdown": "# Capability Registry Spec v1\n\n## Vision\n\nGenome's Agentic OS should make installed capabilities discoverable from the visible OS root. Commands, skills, MCP servers, hooks, plugins, libraries, and shared rules are not considered usable because files exist in hidden harness folders. A capability becomes active only through a deterministic install state machine: declared package intent is validated, visible artifacts are linked or copied, harness adapters are generated from registry state, doctor checks are recomputed, and the final active set is written as generated evidence.\n\nThe registry model gives Codex, Claude, and future harnesses one shared source of truth. Harness-specific folders such as `.codex/` and `.claude/` are generated adapters. They may cache or render configuration, but they do not define what the OS contains. `agentic-os.lock.json` is also generated evidence, not authority: install and doctor always recompute current registry, artifact, adapter, and check state before comparing that recomputed state to the previous lockfile for drift.\n\n## Architecture\n\nThe installed OS root contains `.agentic_root`, package metadata, generated lock metadata, visible capability folders, and registry files under `registries/`. The source package provides templates, schemas, installers, adapter generators, and validation fixtures. The installed runtime state is represented by visible root-level folders plus canonical registry files. Hidden harness folders and the lockfile are generated outputs.\n\n### Registry Authority\n\nPer-type registry files are the canonical authoring surface. `registries/capabilities.yml` is generated from the per-type registries and must never be hand-authored during normal install. The installer computes a normalized capability graph from `registries/commands.yml`, `registries/skills.yml`, `registries/mcp.yml`, `registries/plugins.yml`, `registries/libraries.yml`, `registries/hooks.yml`, and `registries/rules.yml`; writes `registries/capabilities.yml`; then re-reads it and verifies that each generated record carries a `source_registry`, `source_id`, and `source_digest` matching exactly one per-type record. If `capabilities.yml` is edited independently, install and doctor fail with `capability_digest_drift` unless the command is an explicit migration repair mode that rewrites it from per-type registries.\n\nThe canonical registries are:\n\n| Registry | Path | Responsibility |\n| --- | --- | --- |\n| Capability registry | `registries/capabilities.yml` | Generated normalized installed capability index, status, source registry pointer, and source digest. |\n| Command registry | `registries/commands.yml` | Slash commands, executable shims, aliases, input/output contracts, and harness targets. |\n| Skill registry | `registries/skills.yml` | Skills, source paths, installed paths, dependencies, update policies, and harness targets. |\n| MCP registry | `registries/mcp.yml` | MCP server launch definitions, env var names, exposed tools, approval mode, and doctor checks. |\n| Plugin registry | `registries/plugins.yml` | Plugin and connector bundle metadata plus emitted harness adapters. |\n| Library registry | `registries/libraries.yml` | Embedded, linked, global, or externally managed libraries used by capabilities. |\n| Hook registry | `registries/hooks.yml` | Lifecycle hooks, trigger events, allowed side effects, network behavior, timeout, and rollback behavior. |\n| Rule registry | `registries/rules.yml` | Shared rule documents consumed by generated `AGENTS.md`, `CLAUDE.md`, and future harness adapters. |\n\n### Schema Ownership\n\nSchemas live in `schemas/registries/` in this source package. The registry schema language is JSON Schema 2020-12, with YAML registries parsed to JSON before validation. Required schema files are `capability.schema.json`, `commands.schema.json`, `skills.schema.json`, `mcp.schema.json`, `plugins.schema.json`, `libraries.schema.json`, `hooks.schema.json`, and `rules.schema.json`. Contributors validate package records with `agentic-os validate --package agentic-os.package.json --registries registries/ --schemas schemas/registries/`. CI must run the same command plus fixture tests for invalid records.\n\nEvery registry item must normalize to a capability record with `id`, `type`, `display_name`, `status`, `visibility`, `customer_safe`, `source`, `install`, `harness_targets`, `provides`, `requires`, `doctor`, `update`, and `source_digest`. Literal secrets are forbidden in registry files; records may reference env var names only. Secret detection is deterministic: schema validation rejects field names or scalar values matching configured secret patterns such as private key blocks, token-like values longer than 32 characters in secret-bearing fields, and literal values under `env` where only variable names are allowed.\n\n### Capability Lifecycle\n\nThe only allowed lifecycle statuses are:\n\n| Status | Meaning | May appear in inventory | May emit adapter behavior | May appear in lock summary |\n| --- | --- | --- | --- | --- |\n| `declared` | Package intent exists and schema validation passed, but visible artifact work has not completed. | yes | no | yes |\n| `linked` | Visible artifact was copied or linked at `install.target`. | yes | no | yes |\n| `generated` | Harness adapters and inventory were generated from registry state. | yes | no, unless doctor passes | yes |\n| `active` | Registry, artifact, adapter extraction, and doctor checks all passed in the current run. | yes | yes | yes |\n| `broken` | A declared capability failed artifact, adapter, or doctor validation. | yes | no | yes |\n| `disabled` | Operator or package policy intentionally disables the capability before adapter generation. | yes | no | yes |\n| `removed` | A previously locked capability is no longer declared and was removed by managed upgrade. | yes for one lock cycle | no | yes |\n\nStatus transitions are installer-owned. Fresh install begins at `declared`, moves to `linked` after visible artifact creation, moves to `generated` after adapter and inventory generation, and moves to `active` only after doctor recomputes and passes all checks. Any failed required step transitions the capability to `broken`; `broken` and `disabled` records are visible in inventory but cannot emit enabled adapter entries. A capability cannot move directly from `declared` to `active`.\n\n### Install Modes\n\n| Mode | Input condition | Failure policy | Migration behavior |\n| --- | --- | --- | --- |\n| `fresh-install` | Empty or absent OS root. | Fail on any invalid registry, artifact, adapter, doctor, or customer-safety check. | None. |\n| `managed-upgrade` | Existing root with valid prior lockfile and registries. | Fail on registry drift, unregistered adapter behavior, unsafe local overrides, or doctor failures unless capability is explicitly disabled. | Removed capabilities are marked `removed` for one lock cycle. |\n| `legacy-migration-report` | Existing hidden harness state without valid registry authority. | Never writes adapters or lockfile; reports proposed registry records and blockers. | Read-only proposal only. |\n| `legacy-migration-apply` | Operator accepts a report file. | Writes registries first, then runs the same gates as managed upgrade. | Hidden harness state is imported only if represented as registry records. |\n\n### Local Override Policy\n\n`agentic-os.local.json` may only set machine-local paths, explicit disabled capability IDs, non-secret env var name mappings, and adapter output locations. It may not change `visibility`, `customer_safe`, `type`, `provides`, `harness_targets`, launch commands, hook side effects, network behavior, doctor checks, or update policy. Install and doctor validate overrides before registry normalization. A forbidden override fails with `unsafe_local_override` and cannot be downgraded to a warning.\n\n### Adapter Extraction Gate\n\nAdapter generation is deterministic: it reads registry files, renders adapter outputs, then runs an extractor for each generated adapter. Extractors produce typed active items: `command`, `skill`, `mcp_server`, `plugin`, `hook`, `library`, and `rule`. TOML, JSON, and YAML adapters are parsed structurally. Markdown adapters such as `AGENTS.md` and `CLAUDE.md` are parsed using reserved generated blocks bounded by `<!-- agentic-os:begin <type> <id> -->` and `<!-- agentic-os:end <type> <id> -->`; active slash commands, rule includes, skill references, MCP references, and hook references may only appear inside those generated blocks. Free prose outside generated blocks is allowed only as explanatory text and must not contain active command declarations, include directives, hook trigger declarations, or harness config snippets. The Markdown extractor fails on any reserved active syntax outside a generated block.\n\nEach extracted active item must map to exactly one normalized capability record with matching `id`, `type`, `harness_target`, and source digest. Install and doctor fail if an adapter contains an enabled command, rule, MCP, hook, plugin, library, or skill reference that cannot be traced to exactly one canonical registry record. Zero unregistered active adapter items are permitted.\n\n### Inventory And Lockfile\n\n`INVENTORY.md` is generated from the recomputed capability graph and is optimized for scanning. It must include every capability ID, type, status, visibility, customer-safe flag, primary path, source registry, and doctor summary. Active capabilities are shown separately from declared, disabled, broken, and removed capabilities. The generator fails if the inventory omits any normalized record or marks a non-active capability as usable.\n\n`agentic-os.lock.json` is generated-only evidence. It records the recomputed capability graph digest, per-type registry digests, installed artifact fingerprints, adapter output digests, extractor results, lifecycle statuses, and doctor result summaries from the current run. It is never used as proof that the current install is valid. Doctor always recomputes current state first, then compares recomputed digests against the lockfile and reports `lockfile_drift` for stale or hand-edited lockfiles.\n\n## Phases\n\n### P1 - Registry Schema And Installer Gate (2 weeks)\n\nDefine JSON Schema 2020-12 schemas for all registry files and the normalized capability record. Add installer logic for `fresh-install` and `managed-upgrade` that reads `agentic-os.package.json`, validates per-type registries, writes generated `capabilities.yml`, verifies source digests, applies allowed local overrides, installs or links visible folders, and refuses to continue if any capability cannot enter a valid lifecycle status.\n\nDeliverables: schema files in `schemas/registries/`, sample registry files, lifecycle transition code, per-type-to-capability normalization, digest drift checks, local override validation, contributor command `agentic-os validate`, and failing fixtures for missing fields, secret-like values, unresolved paths, unsafe overrides, edited `capabilities.yml`, and unsupported capability types.\n\n### P2 - Inventory And Harness Adapter Generation (2 weeks)\n\nGenerate `INVENTORY.md`, `.codex/config.toml`, `AGENTS.md`, `CLAUDE.md`, and other adapter outputs from registry state. Add structural and Markdown extractors so generated adapter entries can be traced back to registry records. Dot folders are generated outputs only, and adapter behavior remains disabled until the current doctor run passes.\n\nDeliverables: inventory generator, Codex adapter, Claude adapter, generated-block Markdown format, adapter extractors, adapter provenance comments, tests proving hidden harness state cannot introduce an unregistered active capability, and a fixture where a Markdown adapter template emits an extra slash command and install plus doctor both fail.\n\n### P3 - Doctor, Lockfile, And Migration Path (2 weeks)\n\nImplement `agentic-os doctor`, lockfile writing, and separated legacy migration modes. Doctor recomputes registry, artifact, adapter extraction, inventory, and check state before reading the lockfile for drift comparison. Migration report mode proposes registry records from existing scattered capability locations without writing adapters or lockfile; migration apply mode writes registries and then runs managed-upgrade gates.\n\nDeliverables: doctor command, generated `agentic-os.lock.json` writer, lockfile drift detection, `legacy-migration-report`, `legacy-migration-apply`, compatibility decision notes for `shared_factory/05-knowledge/skills/`, and regression tests for customer-safe filtering, broken lifecycle status, removed lifecycle status, and stale lockfile handling.\n\n## Acceptance Criteria\n\n1. A fresh install creates the expected root shape under `~/agentic_os/`, including `.agentic_root`, package metadata, visible capability folders, `registries/`, generated `capabilities.yml`, generated `INVENTORY.md`, and generated `agentic-os.lock.json`.\n2. Per-type registries are the only canonical authoring surface; `capabilities.yml` is generated and install fails on source digest drift.\n3. The installer fails before adapter generation when any declared capability lacks a valid normalized capability record or attempts an unsafe local override.\n4. A capability emits enabled adapter behavior only in `active` status, and `active` is reachable only after schema validation, visible artifact creation, adapter extraction, inventory generation, and current doctor checks pass.\n5. `INVENTORY.md` includes every normalized capability, separates active from declared, disabled, broken, and removed records, and never advertises a non-active capability as usable.\n6. Generated `.codex/` and `.claude/` adapter entries are derived only from registry records and include provenance that points to the source registry record and digest.\n7. Adapter extractors fail install and doctor when any active command, rule, MCP, hook, plugin, library, or skill reference cannot be traced to exactly one registry record.\n8. MCP records name required env vars but never contain literal secret values.\n9. Customer OS installs fail doctor checks when operator-only capabilities are active or when local overrides attempt to change safety-relevant fields.\n10. Hook records must declare trigger event, command, allowed filesystem roots, external network behavior, timeout, rollback or failure behavior, and customer-install eligibility.\n11. The lockfile is generated-only evidence. Doctor recomputes current state before comparing it to the lockfile and reports stale or hand-edited lockfiles as drift.\n12. Legacy migration report mode is read-only; strict enforcement begins only in fresh install, managed upgrade, and migration apply modes.\n\n## Validations\n\n- Schema tests validate every registry type and the normalized capability record using JSON Schema 2020-12 through `agentic-os validate --package agentic-os.package.json --registries registries/ --schemas schemas/registries/`.\n- Lifecycle tests prove capabilities cannot skip from `declared` to `active`, cannot emit adapters while `broken` or `disabled`, and are marked `broken` on failed required checks.\n- Installer tests cover missing registry files, invalid capability types, unresolved install targets, command resolution failures, literal secret rejection, unsafe local overrides, and edited generated `capabilities.yml`.\n- Inventory tests compare the full normalized capability graph against generated `INVENTORY.md` entries and assert non-active statuses are not presented as usable.\n- Adapter tests parse generated Codex and Claude outputs and assert every enabled entry maps to exactly one registry record; Markdown tests include an extra slash command outside a generated block and must fail.\n- Doctor tests simulate failed MCP startup, missing skills, unsafe hooks, operator-only capabilities in customer installs, stale lockfiles, and lockfile hand edits.\n- Migration tests run in report-only mode first, confirm existing harness files are not treated as source of truth, and verify apply mode runs managed-upgrade gates before writing lockfile evidence.\n\n## Risks\n\n- Existing users may rely on hidden harness folders as de facto source of truth. Mitigation: ship read-only migration report mode before apply mode and preserve generated adapter backups during P3.\n- Registry records can become verbose. Mitigation: keep per-type registries concise and generate normalized capability records programmatically.\n- Adapter drift may reappear if humans edit generated files. Mitigation: include provenance comments, generated-file headers, extractor gates, and doctor checks that compare adapter content to registry state.\n- Customer-safe filtering could be bypassed if capabilities omit visibility fields. Mitigation: make `visibility` and `customer_safe` required fields with no permissive defaults and block local overrides from changing them.\n- Lockfile drift could be mistaken for validity. Mitigation: lockfile is explicitly generated evidence and doctor recomputes current state before reading it.\n\n## What's NOT in v1\n\n- No runtime Notion database dependency. Notion may be a registered MCP/control-plane integration, but registries live in the OS root.\n- No full implementation of every command candidate beyond registry representation, schema validation, adapter extraction, and doctor gates.\n- No final decision on whether commands are Markdown prompts, executable shims, or both.\n- No final decision on whether `shared_factory/05-knowledge/skills/` remains a compatibility mirror or becomes generated from top-level `skills/`.\n- No plugin marketplace implementation beyond registry representation and adapter generation contracts.\n- No support for local overrides that change safety-relevant capability semantics.",
  "responseMap": {
    "respondingToRound": 3,
    "writerModel": "GPT-5 Codex",
    "responses": [
      {
        "issueId": "i1",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Capability Lifecycle",
            "lineRef": "Architecture / Capability Lifecycle",
            "before": "A capability is considered active only when all of these are true: the registry record status is `installed`, the visible installed artifact exists at `install.target`, required commands or env var names are declared, required non-secret runtime values are available to the current environment, and declared doctor checks pass.",
            "after": "Status transitions are installer-owned. Fresh install begins at `declared`, moves to `linked` after visible artifact creation, moves to `generated` after adapter and inventory generation, and moves to `active` only after doctor recomputes and passes all checks.",
            "summary": "Replaced prose installed/active language with an explicit lifecycle state machine and ordered transitions."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i2",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Registry Authority",
            "lineRef": "Architecture / Registry Authority",
            "before": "Every registry item must normalize to a capability record with `id`, `type`, `display_name`, `status`, `visibility`, `customer_safe`, `source`, `install`, `harness_targets`, `provides`, `requires`, `doctor`, and `update`.",
            "after": "Per-type registry files are the canonical authoring surface. `registries/capabilities.yml` is generated from the per-type registries and must never be hand-authored during normal install.",
            "summary": "Picked per-type registries as the canonical source and made capabilities.yml generated with digest enforcement."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i3",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Adapter Extraction Gate",
            "lineRef": "Architecture / Adapter Extraction Gate",
            "before": "Adapter generation is deterministic: it reads registry files, renders adapter outputs, then re-parses those outputs to confirm every enabled adapter entry maps back to exactly one registry record.",
            "after": "Markdown adapters such as `AGENTS.md` and `CLAUDE.md` are parsed using reserved generated blocks bounded by `<!-- agentic-os:begin <type> <id> -->` and `<!-- agentic-os:end <type> <id> -->`; active slash commands, rule includes, skill references, MCP references, and hook references may only appear inside those generated blocks.",
            "summary": "Defined adapter item extraction, Markdown generated blocks, reserved active syntax, and the exact failure condition."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i4",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Local Override Policy",
            "lineRef": "Architecture / Local Override Policy",
            "before": "(v0: local override boundaries were not defined)",
            "after": "`agentic-os.local.json` may only set machine-local paths, explicit disabled capability IDs, non-secret env var name mappings, and adapter output locations. It may not change `visibility`, `customer_safe`, `type`, `provides`, `harness_targets`, launch commands, hook side effects, network behavior, doctor checks, or update policy.",
            "summary": "Added an explicit allowlist and denylist for local overrides with a hard failure code."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i5",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Install Modes",
            "lineRef": "Architecture / Install Modes",
            "before": "Implement `agentic-os doctor`, lockfile writing, and migration from existing scattered capability locations into top-level folders.",
            "after": "| `legacy-migration-report` | Existing hidden harness state without valid registry authority. | Never writes adapters or lockfile; reports proposed registry records and blockers. | Read-only proposal only. |",
            "summary": "Separated fresh install, managed upgrade, migration report, and migration apply with distinct failure policies."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i6",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Schema Ownership",
            "lineRef": "Architecture / Schema Ownership",
            "before": "Define YAML schemas for all registry files and a normalized capability record schema.",
            "after": "Schemas live in `schemas/registries/` in this source package. The registry schema language is JSON Schema 2020-12, with YAML registries parsed to JSON before validation.",
            "summary": "Specified schema location, schema language, required schema files, and the contributor validation command."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i7",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Inventory And Lockfile",
            "lineRef": "Architecture / Inventory And Lockfile",
            "before": "The lockfile records installed capability IDs, versions or source fingerprints, adapter outputs, and doctor result summaries.",
            "after": "`agentic-os.lock.json` is generated-only evidence. It records the recomputed capability graph digest, per-type registry digests, installed artifact fingerprints, adapter output digests, extractor results, lifecycle statuses, and doctor result summaries from the current run. It is never used as proof that the current install is valid.",
            "summary": "Defined the lockfile trust boundary and required doctor to recompute current state before drift comparison."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      },
      {
        "issueId": "i8",
        "priorStatus": "OPEN",
        "disposition": "ACCEPTED",
        "specChanges": [
          {
            "section": "Capability Lifecycle",
            "lineRef": "Architecture / Capability Lifecycle",
            "before": "The spec names `installed` and active behavior but does not define the complete allowed status enum or lifecycle transitions.",
            "after": "The only allowed lifecycle statuses are: `declared`, `linked`, `generated`, `active`, `broken`, `disabled`, and `removed`.",
            "summary": "Added the complete lifecycle status enum and a table showing inventory, adapter, and lockfile behavior per status."
          }
        ],
        "rejectionReason": "",
        "deferralReason": ""
      }
    ],
    "regressionSweep": {
      "i1": "Addressed by lifecycle states and transition ordering; no capability can advertise active behavior before current doctor pass.",
      "i2": "Addressed by making per-type registries canonical and generated capabilities.yml digest-checked.",
      "i3": "Addressed by structural extractors plus Markdown generated blocks and zero unregistered active item policy.",
      "i4": "Addressed by local override allowlist and safety-relevant denylist.",
      "i5": "Addressed by install mode table separating fresh install, managed upgrade, and legacy migration paths.",
      "i6": "Addressed by schema location, JSON Schema version, schema file names, and validation command.",
      "i7": "Addressed by declaring lockfile generated-only evidence and requiring recomputation before drift comparison.",
      "i8": "Addressed by allowed lifecycle status enum, transition triggers, and status behavior table."
    },
    "metaNote": "This revision narrows the spec around enforceable state transitions, registry authority, adapter extraction, lockfile trust, and install modes while preserving the top-level Capability Registry topic."
  },
  "templateChecklist": {
    "hasVision": true,
    "hasArchitecture": true,
    "hasPhases": true,
    "hasAcceptanceCriteria": true,
    "hasValidations": true,
    "hasRisks": true,
    "hasOutOfScope": true
  }
}
