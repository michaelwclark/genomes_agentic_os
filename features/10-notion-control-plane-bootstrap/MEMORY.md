# Memory

- Bootstrap apply must require both verified workspace and explicit parent page ID.
- Without a configured Genome parent page, the safe output is a local manifest, not a fallback Notion page.
