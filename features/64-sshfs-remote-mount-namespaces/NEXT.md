# Next: Feature 64 SSHFS Remote Mount Namespaces

1. Run a short spec duel or project-lead review on `SPEC.md`.
2. Decide the default mount namespace path for macOS and Linux.
3. Implement path detection and translation before adding mount commands.
4. Add dry-run CLI and tests with fake command runners.
5. Roll out to `losmon` only after dry-run output is reviewed.
