# Holdout QA Results

- `uv run --extra dev pytest -q`: 39 passed in 3.45s.
- Config inventory source-reference check: passed. Required official source
  URLs, local version evidence, layer names, prompt file names, and
  security-sensitive key terms were present.
