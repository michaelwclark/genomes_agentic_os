# Judgment

The inventory should be treated as a research base, not an installer.

The official docs and local alpha CLI agree on the core model: layered config,
profiles, project trust, `AGENTS.md` discovery, MCP in config, hooks, sandbox,
permissions, and OTEL. Some schema keys and app/desktop behavior remain
surface-dependent and should be validated again before generating host-mutating
installers.
