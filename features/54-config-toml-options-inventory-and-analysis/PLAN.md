# Plan

1. Verify current official Codex docs and local CLI behavior.
2. Add a Codex `config.toml` inventory under `docs/07-agent-surfaces/`.
3. Add a machine-readable Agentic OS layer map under `templates/agent-config/`.
4. Run tests.
5. Run a source-reference check for required docs, source URLs, and key terms.
6. Commit the branch for orchestrator merge.
