# Judgment

This feature is documentation-only. No source code changes were required.

The guide documents the actual CLI surface: `chain` supports list, test, and
doctor; chain rules are created or edited as file-backed registry entries. That
keeps the documentation source-faithful and avoids inventing a non-existent
`chain create` command.
