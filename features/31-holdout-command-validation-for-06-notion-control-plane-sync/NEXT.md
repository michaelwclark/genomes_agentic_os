# Next

- Keep future live Notion write tests separate from this local mapping holdout.
- If sync output adds an explicit `dry_run` or `applied` boolean later, update
  the result assertions to check those fields directly.
