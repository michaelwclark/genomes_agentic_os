# Investigation

Feature 13 is implemented through installed reference templates, runtime
reference copies, context builder behavior, and validation checks.

The guide needed to explain:

- `shared_factory/05-knowledge/templates/reference/`
- `shared_factory/05-knowledge/references/`
- required reference files
- context packet inclusion of shared references
- shared skill registry and runtime skill index paths
- validation expectations
