# Judgment

The guide is additive and implementation-oriented.

It keeps reference rules in the shared factory instead of scattering them across
projects, and it keeps Claude/Codex skill alignment tied to the source skill
registry.

No diagram asset was added because the runtime path table and context flow are
sufficient for this guide.
