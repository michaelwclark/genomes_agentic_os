# Memory

- Feature-guide documentation should live under `docs/13-feature-guides/` when
  the subject is a completed Build Runner feature rather than a general OS
  concept.
- A documentation follow-up card can mark CLI command help as not applicable
  when the source feature introduced no dedicated command, but it must document
  the adjacent commands users actually run.

