# Memory

Connected source watchers are file-backed under `shared_factory/00-control-plane/` and write source events under `shared_factory/06-runs-and-logs/source-events/`.

Keep new watch sources disabled or dry-run until workspace identity, cursor, idempotency key, and route/context contracts are verified.
