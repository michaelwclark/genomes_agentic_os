# Plan

1. Run the full CLI scaffold test suite.
2. Create a disposable runtime root.
3. Run `agentic-os losmon validate --root <root> --repo <repo>`.
4. Run `agentic-os validate --root <root>`.
5. Confirm project, workflows, automation, run logs, and comparison artifact.
