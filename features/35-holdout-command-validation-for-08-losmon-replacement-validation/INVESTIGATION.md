# Investigation

Feature 08 is implemented in `src/genomes_agentic_os/losmon.py` and exposed as
`agentic-os losmon validate`.

The holdout verifies the generated runtime shape rather than only checking the
command output.
