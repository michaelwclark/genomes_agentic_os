# Judgment

Ready to merge.

The feature keeps automation maturity conservative by default. It only allows `observe` and `prepare` without evidence, and it records reviewable state changes in domain/project markdown.
