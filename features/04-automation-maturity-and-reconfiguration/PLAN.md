# Plan

1. Add automation maturity operations for readiness checks, project attachment, and maturity transitions.
2. Wire `automation check`, `automation attach`, and `automation set-maturity` into the CLI.
3. Add tests for default observe maturity, blocked unsafe promotion, evidence-backed promotion, and project attachment.
4. Run full pytest and temp-root automation smoke validation.
