# Investigation

- Existing automation scaffolds already include `Level` in `automation.md` with default `observe`.
- Project records from feature 01 provide `status.md` and `source-map.md`, which are enough for reviewable attachment writebacks.
- Domain `00-control-plane/decisions.md` is the right local file for maturity-change audit rows.
- The plan excludes schedulers and unattended production writes, so the implementation stays filesystem-local.
