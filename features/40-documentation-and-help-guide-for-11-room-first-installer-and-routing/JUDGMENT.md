# Judgment

The guide is implementation-oriented and consistent with prior feature guides.

It keeps Notion in the control-plane role by describing filesystem profile
installation rather than implying that Notion is the runtime database. It also
keeps Claude and Codex aligned by documenting the shared `ROUTER.md` pointer
contract.

No diagram asset was added because the plain text operational flow is clearer
and avoids unnecessary generated media.
