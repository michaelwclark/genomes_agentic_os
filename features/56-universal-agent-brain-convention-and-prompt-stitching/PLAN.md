# Plan

1. Add a durable guide under `docs/07-agent-surfaces/`.
2. Add reusable `BRAIN.md`, `ROUTER.md`, `CONTEXT.md`, and stitching map
   templates under `templates/agent-config/`.
3. Verify the guide and templates mention all required prompt files, file
   classes, migration rules, and nested stitching examples.
4. Run the repository test suite.
