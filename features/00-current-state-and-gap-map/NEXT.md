# Next

Next lowest READY prefix after this card:

- `01 Project Create And Active Work`
- Card ID: `366683b4-8dab-81e2-9268-ea82e66315a2`

Do not start the next card in this run unless the user explicitly asks for the full queue or another feature.

Before feature `01`, resolve how to handle the already-dirty root worktree. A clean commit or an explicit instruction to continue in the dirty baseline would make branch/worktree handling safer.
