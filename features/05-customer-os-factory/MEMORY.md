# Memory

- Customer OS generation must avoid `create_domain` because that initializes the source-owner default domains.
- Customer validation should be profile-aware and should not require the personal default domain list.
