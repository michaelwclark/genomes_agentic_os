# Investigation

Feature 02 introduced routing and context command paths through
`src/genomes_agentic_os/routing.py` and `src/genomes_agentic_os/cli.py`.

Documented command surfaces:

- `agentic-os route`
- `agentic-os context build`
- `agentic-os here route`
- `agentic-os here context build`

The guide belongs in `docs/13-feature-guides/` because it explains a completed
Build Runner feature rather than changing the global operating model.

