# Holdout QA

- Run `uv run --extra dev pytest -q`.
- Run a disposable project-create smoke test using `agentic-os init`, `agentic-os project create`, and `agentic-os validate`.
- Confirm `docs/13-feature-guides/01-project-create-and-active-work.md` exists and references feature 01 source artifacts.
