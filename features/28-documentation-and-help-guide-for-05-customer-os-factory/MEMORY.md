# Memory

The customer OS factory guide should stay tied to the public `agentic-os
customer` command group and to `customer_profiles/example-customer.yml`.

The key distinction for operators is that `core_errors` are blocking runtime
shape failures, while `profile_warnings` include optional profile gaps, domain
warnings, and private source-owner term findings.
