# Holdout QA Results

Passed.

- `uv run --extra dev pytest -q`: `14 passed in 1.03s`.
- Temp-root smoke created a linked repo project, routed `Deploy losmon_replacement`, and built context from inside the linked repo.
