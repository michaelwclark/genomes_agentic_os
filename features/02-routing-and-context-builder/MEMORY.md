# Memory

- Keep routing deterministic and read-only.
- Use project `sources.repo` to map external working directories back to installed project records.
- Treat destructive, production, external, customer, secret, billing, legal, deploy, and merge terms as approval risks.
