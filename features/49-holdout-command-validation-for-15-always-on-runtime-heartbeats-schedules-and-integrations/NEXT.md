# Next

- Merge feature 49 after review.
- If future runtime holdouts need install setup, use `agentic-os init --target <root>` for the base root and reserve `--root` for installed-root operations.
- No feature 15 source follow-up is required from this holdout.
