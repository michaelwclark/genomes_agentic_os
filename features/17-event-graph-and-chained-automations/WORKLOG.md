# Worklog

- Added event envelope, ledger, chain rule, processing result, and dead-letter templates.
- Added `os-event`, `os-chain`, and `event-graph-operator`.
- Added `event_graph.py` with append, list, process, replay, chain test, chain doctor, queue writes, and dead-letter handling.
- Added `run-log close --emit-events`.
- Added tests and temp-root smoke.
