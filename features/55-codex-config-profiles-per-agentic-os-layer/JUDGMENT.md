# Judgment

Keep native Codex profile keys in TOML and richer Agentic OS metadata in YAML.
This avoids pretending that every OS concern is a Codex-native setting while
still making the installer and docs precise.
