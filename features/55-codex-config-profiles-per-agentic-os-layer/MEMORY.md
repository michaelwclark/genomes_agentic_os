# Memory

Use `templates/agent-config/codex-profile-manifest.yml` as the machine-readable
profile metadata source. Use `templates/agent-config/codex-profiles.toml` as
the Codex-facing profile template.
