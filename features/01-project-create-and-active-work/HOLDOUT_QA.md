# Holdout QA

- Run `uv run pytest -q`.
- Create a temp root.
- Run `agentic-os init`.
- Run `agentic-os project create los losmon_replacement`.
- Validate the temp root.
- Confirm rerun preserves local edits.
