# Plan

1. Add project status constants and project scaffold renderers.
2. Add append-only helpers for active-work, project index, and source-map rows.
3. Add `create_project` to scaffold.
4. Add `agentic-os project create` CLI parser and handler.
5. Add tests for creation, idempotency, invalid names, and domain aliases.
6. Run tests, smoke project creation in a temp root, and validate the temp root.
