# Memory

- `agentic-os project create` remains additive on rerun and preserves manual
  status edits.
- The `lenders` domain alias still writes project state under `los`, not a new
  `lenders` domain.

