# Summary

Feature 21 confirms the project-create surface still behaves as feature 01
claimed. A disposable OS root accepted project creation, preserved a manual
status edit on rerun, wrote source references, normalized `lenders` to `los`,
rejected an invalid project slug, and passed validation.

