id: cdfa50ee-05bc-40ec-bb17-133a9d272422
title: 60 Memory Driven Toolsmith Loop
prefix: "60"
slug: 60-memory-driven-toolsmith-loop
source_queue: Local
status: ready_to_merge
created_at: 2026-05-30T03:09:13Z
acceptance:
  - self-improvement analysis runs from durable evidence, not opaque model state
  - dry-run output explains repeated friction, missed tool/workflow/automation opportunities, and proposed fixes with source evidence
  - apply mode writes reviewable local proposals only; it does not mutate harness tools, skills, or Notion without an explicit approval path
  - proposal records can target project rules, shared references, skills, commands, workflows, automations, validation checks, tool wrappers, or new feature specs
  - dedupe, cooldown, confidence, and redaction safeguards prevent noisy or unsafe self-modification
  - runtime scheduling can trigger the analyzer through existing supervisor and schedule surfaces
  - default installed OS includes the self-improvement workflow, command, reviewer skill, templates, config, proposal directory, and disabled-or-dry-run schedule target
  - proposed changes that affect referenced skills, commands, workflows, or automations include a reference-migration plan
verification:
  - command: node /Users/genome/.claude/skills/duel/duel.mjs --idea SPEC.md --context PRE_DUEL_BRIEF.md --writer-runner claude-cli --critic-runner codex-cli --max-rounds 4
    result: stopped before round 1; local Claude CLI returned credit balance too low
  - command: node /Users/genome/.claude/skills/duel/duel.mjs --idea SPEC.md --context PRE_DUEL_BRIEF.md --writer-runner codex-cli --critic-runner codex-cli --writer-model default --critic-model default --max-rounds 4 --duel-id 2026-05-31-codex-local
    result: PASS in 3 rounds; final spec at .duels/2026-05-31-codex-local/final-spec.md
  - command: .venv/bin/python -m pytest -q tests/test_cli_scaffold.py -k 'self_improvement'
    result: 8 passed, 86 deselected
  - command: .venv/bin/python -m compileall -q src/genomes_agentic_os && .venv/bin/python -m pytest -q
    result: 97 passed in 14.64s
  - command: git diff --check
    result: passed
  - command: rg duplicate scan for self-improvement command functions and schedule ids
    result: one run, one approve, one promote, and one self_improvement_review schedule id
  - command: temp-root smoke covering init, apply, approve, promote, runtime init, schedule run-due, and runtime run-next dispatch
    result: passed in /tmp/aos-self-improvement-final.kFiK3b; apply wrote proposals, approve wrote a content-bound approval, promote wrote a draft feature packet, scheduler dispatch completed
