# Spec

Add an operator-facing guide for feature 16, Connected Source Watch Registry.

The guide must describe the installed runtime surface, control-plane registry
files, CLI commands, dry-run/apply behavior, doctor checks, validation commands,
and source artifacts.

Acceptance criteria:

- `docs/13-feature-guides/16-connected-source-watch-registry.md` exists.
- The guide references real feature 16 files and command surfaces.
- The guide does not use Mermaid.
- Build Runner artifacts exist for feature 50.
- Repository tests pass.
- A guide source-reference check passes.
