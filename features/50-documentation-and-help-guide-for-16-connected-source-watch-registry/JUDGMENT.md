# Judgment

This feature is documentation-only. No source code changes were required.

The guide keeps Notion as a connected/control-plane source and preserves the
repo rule that runtime state is file-backed inside the installed OS. The guide
documents dry-run first operations before apply-mode event and cursor writes.
