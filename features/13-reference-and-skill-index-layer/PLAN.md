# Plan

1. Install reference templates as runtime reference files.
2. Add runtime references to validation.
3. Add shared references to context packets.
4. Add tests for install and context inclusion.
5. Run full pytest and temp-root context/validate smoke.
