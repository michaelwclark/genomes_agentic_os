# Spec

Install and load the shared reference layer.

## Acceptance

- Reference templates are copied into installed OS roots.
- Runtime reference files exist under `shared_factory/05-knowledge/references/`.
- Context builder includes naming, tool index, source priority, and style/output references.
- Validation checks the runtime reference layer.
