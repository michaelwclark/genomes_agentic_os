# Investigation

- Static plan directories and templates already exist and are installed by docs update.
- The missing behavior is a deterministic capture path that future agents can use instead of chat-only notes.
