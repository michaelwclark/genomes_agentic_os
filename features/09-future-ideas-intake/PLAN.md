# Plan

1. Add plan capture helper with OS, domain, and project routing.
2. Wire `plan capture` into the CLI.
3. Add tests for each routing target.
4. Run full pytest and temp-root plan capture smoke validation.
