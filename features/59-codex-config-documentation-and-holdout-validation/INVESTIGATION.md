# Investigation

The closeout must cover documentation and actual command execution. The holdout
matrix caught and fixed a TOML merge-order bug where confirmed conflict merges
could append root keys after section headers. A regression assertion was added.
