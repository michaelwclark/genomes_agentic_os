# Plan

1. Refresh CLI/install docs with TOC, how-to flows, examples, and a durable SVG.
2. Execute every config layer through dry-run, apply, re-run, and doctor.
3. Execute conflict, backup, and missing-config validation paths.
4. Add a validation log and summarized closeout.
5. Run the full test suite and content checks.
