# Holdout QA

- Confirm the guide has a table of contents.
- Confirm command examples include `workflow check`, `run-log create`, and
  `run-log close`.
- Confirm no Mermaid diagram was added.
- Run the repository tests.

