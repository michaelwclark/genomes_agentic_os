# Worklog

- Added the config install operation module.
- Added the CLI subcommand.
- Added tests for dry-run, apply, idempotency, conflict, backup, missing
  directory, and prompt-file layers.
- Added the installer guide.
- Ran full pytest and a direct dry-run holdout command.
