# Memory

`agentic-os config install` is the canonical command for installing or merging
Codex `config.toml` conventions into Agentic OS directories. It preserves local
values, requires confirmation for conflicts, and can back up existing configs.
