# Plan

1. Add `src/genomes_agentic_os/config_ops.py` for layer templates, prompt file
   creation, merge planning, backups, diffs, and conflict handling.
2. Add `agentic-os config install` to the CLI.
3. Add tests for dry-run, creation, idempotency, conflict confirmation, backup,
   and layer-specific prompt files.
4. Add a short CLI guide.
5. Run the full test suite and a direct dry-run holdout command.
