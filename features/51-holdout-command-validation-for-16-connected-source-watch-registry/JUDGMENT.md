# Judgment

The connected source watch registry holdout passed.

The command surface remains dry-run first. Apply mode writes local source event
and cursor files, while malformed watch-source registry state fails doctor with
blocker findings.
