# Plan

1. Add a feature-local validation script under `scripts/`.
2. Have the script inspect feature 00 artifacts, `RUN_STATE.json`, source plans, and a temporary installed runtime.
3. Document the holdout command and run it with the existing pytest suite.
