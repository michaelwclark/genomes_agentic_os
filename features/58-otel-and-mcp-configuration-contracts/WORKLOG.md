# Worklog

- Added OTEL env var references and MCP secret policy to generated config.
- Added `config doctor`.
- Added tests for valid and missing contract keys.
- Added docs and a contract template.
