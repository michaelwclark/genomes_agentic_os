# Plan

1. Extend the config template with OTEL env-var references and MCP secret
   policy.
2. Add `agentic-os config doctor`.
3. Add tests for valid installed config and missing OTEL/MCP keys.
4. Add docs and a machine-readable OTEL/MCP contract template.
5. Run the full test suite and focused doctor checks.
