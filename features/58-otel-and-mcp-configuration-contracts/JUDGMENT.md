# Judgment

Telemetry and MCP settings are security-sensitive, so the default is explicit
env var names, no inline secret values, and doctor output that tells the
operator what to add without exposing credential material.
