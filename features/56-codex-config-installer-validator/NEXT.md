# Next

- After feature 55 lands, decide whether `codex-profiles.toml` and `codex-profile-manifest.yml` should move from optional warnings to required preflight files.
- Link `docs/03-installation/installer-validation.md` from the broader docs index if a future docs-nav pass adds one.
