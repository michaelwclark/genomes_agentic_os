# Memory

- The current repo does not have a root `install.py`; the install surface is the `agentic-os` console script.
- `docs/03-installation/` did not exist, so this feature creates it as a focused validation doc entry while preserving `docs/10-cli-and-install/README.md`.
- Optional Codex profile-layer assets are warning-level because feature 55 owns the canonical profile templates.
