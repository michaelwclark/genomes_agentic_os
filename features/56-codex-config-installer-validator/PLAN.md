# Plan

1. Add source-package validation objects and required/optional Codex config source lists.
2. Expose the validator through `agentic-os validate-source`.
3. Add tests proving required files fail and optional layer files warn.
4. Add installation documentation with read-only command examples.
5. Run full pytest plus direct CLI validation.
