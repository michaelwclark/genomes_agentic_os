id: 88681991-7233-4270-8196-fab4f05e2357
title: 62 Role Aware Codex Config Layers
prefix: "62"
slug: 62-role-aware-codex-config-layers
source_queue: Local
status: ready_to_merge
created_at: 2026-06-02T00:00:00Z
acceptance:
  - config generation uses one authoritative policy map for all seven existing Codex layer tokens
  - compatibility discovery records existing public/generated profile names before any profile rename and renders explicit tested aliases when needed
  - navigation/routing layers generate gpt-5.4-mini with medium reasoning while project, workflow, and automation layers generate gpt-5.5 with high reasoning
  - every generated main layer has prompt-visible role identity through PROFILE.md or a documented fallback prompt block
  - config/codex-profile.yml is generated from the same policy as config.toml and the loader-visible prompt inventory
  - config install and install-tree preserve local edits, conflict behavior, MCP no-inline-secret policy, and dry-run defaults across config.toml, PROFILE.md, and config/codex-profile.yml
  - customer OS generation receives customer-safe role/model policy with split full-artifact and user-visible role-text privacy scans
  - docs and validation prove model-tiered roles, managed ownership, customer scan modes, and temporary-root prompt visibility
verification:
  - command: node /Users/genome/.claude/skills/duel/duel.mjs --idea SPEC.md --context PRE_DUEL_BRIEF.md --writer-runner claude-cli --critic-runner codex-cli --max-rounds 4 --duel-id 2026-06-02-feature-62
    result: stopped before round 1; local Claude CLI exited 1
  - command: node /Users/genome/.claude/skills/duel/duel.mjs --idea SPEC.md --context PRE_DUEL_BRIEF.md --writer-runner codex-cli --critic-runner codex-cli --writer-model default --critic-model default --max-rounds 4 --duel-id 2026-06-02-feature-62-codex-local
    result: PASS in 5 rounds; final spec copied into SPEC.md
  - command: .venv/bin/python -m pytest tests/test_cli_scaffold.py -q
    result: 86 passed in 11.20s
  - command: .venv/bin/python -m pytest -q
    result: 89 passed in 13.21s
  - command: PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_cli_scaffold.py -k 'config_install or customer_init_generates_public_customer_os_from_profile or update_apply_migrates_legacy_root_layout_to_harness'
    result: 12 passed, 82 deselected
  - command: PYTHONPATH=src .venv/bin/python -m pytest -q
    result: 97 passed
  - command: bash .agentic-atlas/tools/validate-cli.sh
    result: 53 OK, 2 GUARDED, 55 commands
  - command: PYTHONPATH=<staged-worktree>/src .venv/bin/python -m pytest -q
    result: 97 passed in detached staged Feature 62 tree
  - command: AOS=<repo>/.venv/bin/agentic-os PYTHONPATH=<staged-worktree>/src bash .agentic-atlas/tools/validate-cli.sh
    result: 53 OK, 2 GUARDED, 55 commands in detached staged Feature 62 tree
  - command: codex debug prompt-input 'feature 62 validation'
    result: current prompt-input rendered the project_orchestrator role block from generated AGENTS.md in a temporary project layer
