# Investigation

Feature 08 is implemented in `src/genomes_agentic_os/losmon.py` and exposed as
`agentic-os losmon validate`.

The command creates `losmon_replacement`, three validation workflows,
`thread_intake`, three waiting validation run logs, and
`losmon-comparison.md`.
