# Investigation

Feature 12 is implemented through the runtime documentation/template install
path in `src/genomes_agentic_os/scaffold.py` and validation checks in
`src/genomes_agentic_os/validate.py`.

The guide needed to explain:

- where templates install in runtime roots
- how factory source material is classified
- customer-facing sanitation rules
- additive `agentic-os docs update` behavior
- the difference between source docs and installed runtime templates
