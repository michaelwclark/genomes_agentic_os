# Genomes Agentic OS 0.6.3

Version 0.6.3 is a patch release that completes the Team PR review-result
handoff repair.

## Fixed

- Keep the helper's successful lifecycle status separate from the canonical
  review outcome.
- Export the canonical `clean` or `findings` outcome to the Fabric projector,
  so a valid current-format receipt cannot be dead-lettered as `succeeded`.

## Rollout requirement

Install the exact released artifact on the controller and the BigMac Fabric
worker, then verify one selected Team PR reaches a terminal projection.
