# Genomes Agentic OS 0.8.1

Version 0.8.1 is the patch release that repairs 0.8.0's version projection
and ships the Team PR retry-nonce runtime.

## Fixed

- Admit the Team PR producer's validated retry nonce, propagate it to the
  installed helper, and retain it in the completion effect so an explicit
  same-head recheck cannot reuse an earlier worker intent or receipt.
- Complete the v0.8.0 Python, Node, static manifest, chart, and lockfile
  version projections so locked CI and the next immutable patch release
  validate from one coherent version.

## Included pull requests

- #249 — Team PR retry-nonce accepted end to end through Execution Fabric.
- #252 — v0.8.0 version-coherence repair; documents why the immutable
  `v0.8.0` tag could not be reused and `v0.8.1` is the next valid release.

## Release and installation

PR #253 published this version. Install this runtime before installing
library v0.14.1, and preserve the prior runtime and Execution Fabric release
as rollback targets.
