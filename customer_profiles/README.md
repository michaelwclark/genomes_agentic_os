# Customer Profiles

Customer profiles are safe, publishable inputs to the customer OS factory. They
describe rooms, boundaries, and enabled capabilities without including secrets
or real customer data. Start with `example-customer.yml` and validate the
rendered target with `agentic-os customer validate`.
