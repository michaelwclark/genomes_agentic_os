# Acme tools

Use the shared Agentic OS CLI for routing, validation, and receipts. Use the
configured tracker adapter for work state. Record exact commands and hashes so
another operator can reproduce or reverse the result.

